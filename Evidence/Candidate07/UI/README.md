# Candidate07 original UI screenshots

The two passing packaged UI attempts retain six original PNGs each at 1600×900 and 1280×720. [Inventory](inventory.json) binds every byte identity and original frame metadata; each size folder retains its actual `checks.json`.

This copy/metadata gate does not establish pixel review. Any later visual report must identify its actual viewed files and limits.
