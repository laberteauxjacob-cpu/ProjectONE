# Candidate06 retained numerical coverage — S1 to S3

Executed source S1: `8b5f32640e1239f7035be4c7c1cc6713f0d8be8e`. Final source S3: `c2c422012f4f219fb5e63ac24294a6ba8428f999`.

**The retained runs were not rerun at S3.** They remain S1 package evidence with
their original six runtime hashes and actual result/log/output bindings.

| Retained S1 run | Actual result | Verified scope |
| --- | --- | --- |
| Machine progression | PASS, exit0,95 assertions,0 failures | All ordered JSON/log assertions match; actual level restart is recorded. |
| Projected aim | PASS, exit0,2708 assertions,0 failures | All480 actual discharge observations: six variants × eight headings × ten cases; unique complete matrix, shot counters1–480 and current evaluated pose for every row. |

The exact commit diff changes only pickup visual emission/light/layout settings, its
design JSON/README, and the separate ONE05WeaponCheck fixture. All other
119 of 121 source files are identical committed bytes,
including both retained drivers and production aim, ballistics, input, GameMode,
scoring, health/damage, power-up authority, machines and inventory. Committed
Config/Content entries are unchanged. The JSON lists both Git blob identities
and actual SHA256/byte lengths for every unchanged source file, the four
changed files and both retained runs.

The pickup change multiplies coloured unlit emission by32 and moves the single
light nearer the floor, now explicitly set to32 lumens with its64cm radius
unchanged. S3 also reflects part positions across Y and reverses local yaw,
retaining positive scales, to correct the skull/numeral orientation under the
ordinary overhead camera. It changes rendering, not pickup
ownership or overlap rules. Corrected visuals, new performance and S3 runtime
integration remain separate checks; this rationale is not binary equivalence
or a claim that the entire S3 package already passed.

The earlier S1 weapon30 attempt exited0 but **completed197 assertions with2 failures**
(195 assertions passed). Both failures were stage7, pistol variants0/1: setup
had already installed a carbine, then selected that owned family as supposedly
unowned. Production correctly rejected the plan. S3 checks eligibility across
all owned slots and requires plan validity/revision in the later committed
reload assertion, removing the invalid-plan coverage gap. The197-check
run is preserved as failed history and is not retained as a pass.

No UE, build, native-input, download or network operation was performed for
this audit. Git access was read-only. This record does not establish S3 visual
approval, continuous playback, audio audition or native-input verification.
